Version 0.1.3 changes the session event date/times to have per-second resolution in notifications so that they are easier to read.

Other improvements:
  * Pin GitHub Action hashes in the test and release workflows to make supply chain attacks more difficult.
  * Verify release tag is on main in release workflow to avoid accidentally releasing by a poorly chosen non-release tag name.
